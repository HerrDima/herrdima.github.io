import { ReactNode } from 'react';
export type LibraryPropsType = {
    children: ReactNode;
    textContent: string;
};
export declare function LibraryComponent(props: LibraryPropsType): JSX.Element;
